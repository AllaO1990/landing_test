import { WidgetToolbarDirective } from './widget-toolbar.directive';

describe('WidgetToolbarDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetToolbarDirective();
    expect(directive).toBeTruthy();
  });
});
