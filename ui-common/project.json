{
  "name": "ui-common",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "ui-common/src",
  "prefix": "ui",
  "projectType": "library",
  "tags": ["common"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "ui-common/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
